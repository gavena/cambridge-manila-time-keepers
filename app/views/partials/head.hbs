<head>
    <title>Cambridge Manila Time and Attendance Management</title>
    <meta name="viewport" content="width=device-width, initial-scale=1"> {{>css}} {{>scripts}}

    {{#if isLocal}}
        {{>local}}
    {{else}}
        {{>prod}}
    {{/if}}
</head>
